; ── Outline / symbol view for RSC ─────────────────────────────
; Show the menu path for menu commands, and the command name for
; global commands.

(menu_command
  (menu_prefix)
  (menu_path (identifier) @name)
) @item

(global_command
  (global_command_name) @name) @item
